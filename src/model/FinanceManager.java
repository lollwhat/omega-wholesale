package model;

public class FinanceManager extends User{

    public FinanceManager(String userID, String username, String password, String firstName, String lastName, String createdAt, String updatedAt){
        super(userID, username, password, firstName, lastName, createdAt, updatedAt);
    }
}
